Goal:
{{goal}}

Repository context:
{{repository_context}}

Constraints:
{{constraints}}

Observed changed paths (authoritative when present):
{{changed_paths}}

Output:
- Emit only a `plan-artifact/0` JSON object.
